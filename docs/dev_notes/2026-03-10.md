### **2026-03-10: Centralized Project Definitions & “Selected Work” Navigation Refinement**

**🎯 Major Achievement**: Centralized project metadata and navigation into shared definitions and constants, and aligned the portfolio around a “Selected Work” concept instead of “Case Studies.” Routing, hero content, and footer navigation now pull from unified sources, with tests updated to lock in the new behavior.

## **Critical Problem Solved**

- Project routing, theming, and navigation labels were previously spread across multiple components, making it harder to update copy or add new projects without touching several files.
- The portfolio used “Case Studies” language in some places and “Selected Work” in others, which could be confusing for visitors and reduced perceived cohesion.
- Tests and components needed to reflect a more declarative, centrally defined notion of the project list to keep navigation and content in sync.

## **Solution Implemented: Centralized Projects & Navigation**

### **1. Unified Project Definition & Routing (GitHub: d368574, 0f2054c)**
- Refactored project components and routing to rely on a centralized project definition, so themes and routes for Bantr, Walkability Index, Replacement Trap, and StormSignal come from a single source of truth.
- Updated `ProjectPageShell` to accept a `projectKey` prop, allowing each project page to look up its configuration and enabling more robust “back” navigation and theming.
- Adjusted `Hero` and `ProjectFooterNav` to consume the shared project definitions, ensuring that selected work links and theming are always consistent with route configuration.
- **Files Changed**: `src/app/App.tsx`, `src/app/projects/Bantr.tsx`, `src/app/projects/WalkabilityIndex.tsx`, `src/app/projects/ReplacementTrap.tsx`, `src/app/projects/StormSignal.tsx`, `src/app/projects/components/ProjectFooterNav.tsx`, `src/app/projects/components/ProjectPageShell.tsx`, `src/app/projects/content/selectedWorkProjects.ts`, `src/test/casestudy-components.test.tsx`, `src/test/project-pages.test.tsx`

### **2. “Selected Work” Language & Navigation Consistency (GitHub: df9e03d, 0f2054c, bf830d2)**
- Standardized terminology from “Case Studies” to “Selected Work” across Navbar, Hero, CaseStudyHero, and related components to better reflect the nature of the portfolio.
- Updated footer navigation and hero copy so “Back to Case Studies” is now “Back to Selected Work,” aligning navigation affordances with the new language.
- Refined descriptions in `Hero`, `Approach`, and `WorkWithMe` to clarify how the projects fit together and what kind of work is represented.
- **Files Changed**: `src/app/components/Navbar.tsx`, `src/app/components/Hero.tsx`, `src/app/components/Approach.tsx`, `src/app/components/WorkWithMe.tsx`, `src/app/projects/components/CaseStudyHero.tsx`, `src/app/projects/components/ProjectFooterNav.tsx`

### **3. Centralized Site Navigation Constants & Test Updates (GitHub: bf830d2, 5ba6006)**
- Introduced centralized site navigation constants so key labels and links are defined once and consumed by `Navbar`, `Hero`, `CaseStudyHero`, and `ProjectFooterNav`.
- Updated tests for nav, case study components, and project pages to assert behavior against the new centralized navigation structure.
- Merged the website review changes via PR #25 to ensure copy, navigation, and tests all reflect the new “Selected Work” framing.
- **Files Changed**: `src/app/content/siteNavigation.ts`, `src/app/components/Navbar.tsx`, `src/app/components/Hero.tsx`, `src/app/projects/components/CaseStudyHero.tsx`, `src/app/projects/components/ProjectFooterNav.tsx`, `src/app/projects/content/selectedWorkProjects.ts`, `src/test/casestudy-components.test.tsx`, `src/test/navbar.test.tsx`, `src/test/project-pages.test.tsx`

## **Key Results**

### **Architecture & Maintainability**
- ✅ **Single source of truth for projects**: Routes, theming, and navigation now pull from centralized project definitions, reducing drift and making it easier to add or adjust projects.
- ✅ **Declarative navigation structure**: Site navigation labels and links live in shared constants, avoiding hardcoded strings sprinkled across components.

### **UX & Copy Consistency**
- ✅ **Clear “Selected Work” framing**: The site consistently uses “Selected Work” to describe the portfolio, setting clearer expectations for visitors.
- ✅ **Aligned CTAs and back links**: Hero, navbar, and footer navigation all point to the same conceptual surface, making it easier to move between overview and detail views.

### **Testing & Reliability**
- ✅ **Tests aligned with new structure**: Updated test suites ensure that centralized navigation and project definitions behave as expected across pages and components.

## **Technical Implementation Details**

### **ProjectKey & Selected Work Definition**
- Each project page now passes a `projectKey` to `ProjectPageShell`, which pulls theme and routing details from `selectedWorkProjects`, simplifying per-project components.
- `selectedWorkProjects` was expanded to hold the single source of truth for slug, title, theming, and navigation metadata, which both the hero and footer navigation can consume.

### **Navigation Constants & Tests**
- `siteNavigation` constants encapsulate key labels and paths, which are imported by `Navbar`, `Hero`, `CaseStudyHero`, and `ProjectFooterNav`.
- The navbar and project tests were updated to assert against `siteNavigation` rather than ad hoc strings, keeping tests resilient to internal refactors while still catching user-facing changes.

## **Potential Next Steps**

### **Immediate (Next 1–2 weeks)**

- [ ] Add a small, centralized type definition for project metadata to make `selectedWorkProjects` easier to extend safely.
- [ ] Audit remaining copy for any remaining “Case Studies” references and align them with “Selected Work.”

### **Short-term (Next 1–2 months)**

- [ ] Explore exposing selected work metadata to a future blog or writing surface so project links stay in sync across the entire site.

## **Key Takeaways**

**Major Wins:**
1. Project routing, theming, and navigation now rely on centralized definitions, reducing duplication and future maintenance overhead.
2. The portfolio’s “Selected Work” story is consistent across hero, navbar, and project surfaces, making navigation more intuitive.

**Technical Excellence:**
1. Centralized `selectedWorkProjects` and `siteNavigation` definitions move the codebase toward a more declarative, data-driven architecture.
2. Test updates ensure that the new structure remains stable while still enabling fast iteration on copy and layout.

**Ready for Production:** Today’s changes are structural but well-covered by tests and focused on consistency; they are safe to ship and make future additions to the portfolio markedly simpler.

---

## GITHUB COMMIT SUMMARY

**Total Commits**: 5  
**PRs Merged**: PR #25 (website-review)  
**Key Contributors**: Grant Geist, ghgeist

### Commit Highlights

- `5ba6006`: Merge pull request #25 from ghgeist/website-review
- `bf830d2`: Update navigation labels and links to use centralized site navigation constants; adjust components and tests accordingly.
- `d368574`: Refactor project routing and component structure to use a centralized project definition and update Hero/ProjectFooterNav for dynamic routing.
- `0f2054c`: Refactor project components to include `projectKey` in `ProjectPageShell`, update Hero to use `selectedWorkProjects`, adjust Footer, and change “Back to Case Studies” to “Back to Selected Work.”
- `df9e03d`: Refine text descriptions across components and update “Case Studies” to “Selected Work” in Navbar and related components.

**Note**: Full commit history available via `git log --since=\"2026-03-10 00:00:00\" --until=\"2026-03-10 23:59:59\"`

