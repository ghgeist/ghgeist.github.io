title: Grant Geist
email: granthgeist@gmail.com
description: >- 
  Write a description of your site here. This will appear 
  in your document head meta for search results.
baseurl: "" # leave empty if hosting at root of domain
url: "https://grantgeist.github.io"

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Exclude files from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor 